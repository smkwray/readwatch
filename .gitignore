# Agent orchestration (contract, state, delegates) — private, never tracked
do/
CLAUDE.md
AGENTS.md

# Internal docs not for the repo
docs/

# Environment
.env

# Plans
*.plan.md
plan.md
plans/

# Data and generated outputs (project-local working state)
/data/
/var/
/outputs/
/tmp/

# Go build output
/dist/
*.exe
*.test

# Delegate worktrees (device-local, ephemeral)
.worktrees/

# Python artifacts (tools/ only; defense in depth)
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Editor / OS
.DS_Store
.vscode/
.idea/


# Local orchestration scaffolding from the project template. Kept on disk so the
# pre-commit hook keeps working; never part of the public surface.
/scripts/control_check.py
/scripts/hygiene_check.sh
/.githooks/
/.claude/
